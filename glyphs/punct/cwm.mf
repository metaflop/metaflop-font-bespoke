% box dimension definition %
beginadjchar(23,0,cap#,0);

% sidebearings %
numeric l,r;
normal_adjust_fit(space*0.3u#,space*0.3u#);

% construction %
x1=x2=leftstemloc;
y1=0;
y2=h;

% draw %
undraw z1--z2; %just for fontforge

penlabels(range 0 thru 30);

endchar;